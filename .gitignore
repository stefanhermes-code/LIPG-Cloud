# Runtime files
data/posts.json
data/users.json
data/customer_config.json
# Note: data/auth.json and data/companies.json are tracked in git as the source of truth

# Secrets (contains sensitive API keys)
.streamlit/secrets.toml

